{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "start": "cd ../python-backend && (python3 -m pip install -r requirements.txt || pip3 install -r requirements.txt || pip install -r requirements.txt) && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
